# SSL/TLS 覆盖研究设计

研究不改扫描规则。建立矩阵：Web/HTTPS 识别、TLS 协议与密码套件、证书、具体漏洞四类；每格记录代码/配置入口、Nmap NSE、Nuclei tag、外部模板版本、默认是否执行、风险与验证方式。结论仅依据锁定或实际支持的模板版本；外部模板未固定时标为条件覆盖。

对每个候选规则列出目标、触发指纹、主动性、授权条件、超时/负载、误报边界。产物为 `docs/research/` 文档和可独立验收的建议，不直接创建或修改规则。
