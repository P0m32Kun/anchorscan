# 01 — 归档完成计划

**What to build:** 将所有 ticket 已完成的计划整体移到 `docs/plans/archive/`；为两个缺失状态字段的历史 ticket 补齐 `done`。

**Blocked by:** 无

**Status:** done

**Spec:** `docs/plans/archive/documentation-hygiene/spec.md`

- [x] 确认 `deepen-reportview` 和 `deepen-targetscan` 使用既有中文 `状态：done` 格式。
- [x] 移动 15 个完成计划目录到 `docs/plans/archive/`，保留目录和文件内容。
- [x] 确认 `docs/plans/` 仅保留本计划，且 Markdown 相对链接仍有效。
