# Ticket 05 验收报告：发布默认 catalog、文档与迁移验收

- 日期：2026-08-05
- 范围：`docs/ticket-05-brief.md`（本文件即任务书）、`docs/plans/catalog-json-knowledgebase/tickets/05-package-default-docs-and-acceptance.md`、catalog v2 spec §4/§7/§8
- 风险等级：中（发行物组装 + 默认配置语义变化 + 文档一致性；无新扫描执行路径）
- 执行说明：本票为知识库 v2 对接收官票。工作区既有未提交改动（ticket 04 产物、`safety_gate_test.go`/`tools.go`/`ticket-04-report.md` 等）全部保留，未推倒重写；本票新增/修改见下。

## 改动摘要

**发行物**
- 新增 `config/catalog.json`：冻结 producer artifact（`handbook-v3/dist/catalog.json`，commit 57d739e）字节级拷贝，SHA-256 `7d8ce203a503f63b8d733e6c07fa10c2f1bbb1daf4d5c0619b61e553f374224e`，即 testdata 已固化的那份（`cmp` 逐字节一致）。
- `Makefile`：`RUNTIME_CONFIG` 加入 `catalog.json`，`make package` 自动拷贝并校验归档内非空存在（含既有空文件与 tar 成员双重检查）。
- `config/default.yaml.example`：`knowledge_base.path: catalog.json`（相对配置文件目录解析 → 包内 `config/catalog.json`），并附注释说明协议版本、外部 JSON/Markdown 差异、外部更新步骤与恢复方式。
- `internal/config/init.go`：`defaultConfig()` 生成默认配置时写入 `knowledge_base.path: catalog.json`，全新安装自动指向包内 catalog（此前为空 → 禁用）。

**测试**
- `scripts/package_smoke_test.go`（packageintegration）：
  - 归档必须包含 `config/catalog.json`；
  - 归档内 catalog 的 SHA-256 必须等于冻结 producer checksum（`assertPackagedCatalog`）；
  - 解压后默认配置启动：`config.Load` 得到 `path: catalog.json` → `knowledgebase.Load` 为 ready 且 188 条目；再经 `web.NewServer` 发 GET `/kb` 断言状态徽章为 `ready` 且渲染真实条目（`assertDefaultConfigLoadsPackagedCatalog`）。
- 新增 `internal/knowledgebase/catalog_drift_test.go`：`config/catalog.json` 与 `testdata/catalog-v2-real.json` 字节一致且 SHA-256 固定为 producer checksum（防漂移）；默认路径加载 ready、188 条目。
- `internal/config/init_test.go`：新增 `TestInitDefaultKnowledgeBasePointsAtPackagedCatalog`（生成配置默认 path 为 `catalog.json`）。
- `internal/web/knowledgebase_test.go` 新增 4 个 handler 级测试：
  - 外部 JSON 覆盖（`TestKnowledgeBaseLoadsExternalJSONCatalog`：ready + 外部 4 条目 + 详情页渲染 `sources`/`generated` 审计块）；
  - legacy Markdown 回退（`TestKnowledgeBaseLegacyMarkdownLoadsAsLegacyUnknown`：详情页 `legacy-unknown` + "旧 Markdown 未声明 safety"）；
  - 缺失文件（`TestKnowledgeBaseMissingPathShowsUnavailableDiagnostic`：`unavailable` + `no such file` + 零条目，不回退）；
  - 不兼容文件（`TestKnowledgeBaseIncompatibleFileShowsUnavailableDiagnostic`：`unavailable` + `catalog JSON 无效` + 零条目）。
- `internal/web/report_handler_test.go`：`TestReportPageVulnerabilityAggregateExplainsDisabledCatalog` 显式配置空 `knowledge_base.path` 以维持 disabled 分支测试意图（配置缺失时现在自动生成指向 `catalog.json` 的默认配置，属契约预期变化）。
- `scripts/web-smoke.mjs`：config 注入正则适配 example 新增注释行（`knowledge_base:` 与 `path:` 之间允许注释行）。

**spec 契约补齐（验收发现）**
- spec 4.2 要求 `sources`/`generated`"至少在 KB 详情和诊断中保留"，但详情模板未渲染（前票遗漏）。本票在 `knowledgebase_detail.html` 新增"来源与生成信息"审计区块（Sources 列表 + Generated by/at，有值时显示），并加测试断言。

**文档与状态**
- README.md：新增"漏洞知识库（KB）"章节（协议版本 v2 / handbook-v3、发行内置 catalog 与 checksum 追溯、外部更新步骤、unavailable/degraded 诊断不回退、safety/status/legacy 边界、恢复方式）。
- docs/deploy.md：新增"知识库（catalog）运维"章节（归档内容、外部路径与更新、诊断与恢复、升级时旧配置无 path 则保持禁用）。
- internal/web/templates/config.html：placeholder 由 `../Pentest-Playbook/handbook/内网渗透测试手册_v2.md` 改为 `catalog.json 或 /data/catalog.json`，帮助文本补齐协议版本、外部更新、legacy 边界与恢复说明。
- CHANGELOG.md：`[Unreleased]` 记录发行内置 catalog 与诊断行为。
- `tickets/05-package-default-docs-and-acceptance.md`：Status `draft` → `done`。
- `spec.md`：Status `proposed` → `implemented`（仓库惯例：ticket 用 `done`、ADR 用 `accepted`；spec 为已实施落地，采用 `implemented` 并与 §10 实施状态同步）。

## 实测：必需验收

```bash
make test
```

结果：通过（Go 全部包 ok；node --test 19/19）。

```bash
go vet ./...
```

结果：`EXIT=0`（无输出）。

```bash
make pr-check
```

结果：`EXIT=0`（test、doc-check、docx-test、build、package-test、web-smoke 全链路通过；packageintegration `ok .../scripts 6.570s`；`Web browser smoke test passed.`）。

### package smoke 证据（解压 `dist/anchorscan-v2.0.5-8-gdaf7064-dirty-darwin-arm64.tar.gz`）

```text
$ tar -tzf ...tar.gz | grep -E "catalog|default.yaml"
anchorscan-v2.0.5-8-gdaf7064-dirty-darwin-arm64/config/default.yaml.example
anchorscan-v2.0.5-8-gdaf7064-dirty-darwin-arm64/config/catalog.json
$ shasum -a 256 .../config/catalog.json
7d8ce203a503f63b8d733e6c07fa10c2f1bbb1daf4d5c0619b61e553f374224e
$ ./anchorscan web --config config/default.yaml.example --db ... --listen 127.0.0.1:18081
listening on http://127.0.0.1:18081
$ curl -s http://127.0.0.1:18081/kb | grep -oE 'knowledgebase-status">[a-z]+'
knowledgebase-status">ready
$ curl -s http://127.0.0.1:18081/kb | grep -oE 'href="/kb/[^"]+"' | sort -u | wc -l
188
```

默认配置启动加载为 **ready**（188 条目），启动日志仅 `listening on ...` 无错误；归档清单包含 catalog 且 checksum 与 producer artifact 一致。

### handler/config 测试证据

新增 4 个 handler 测试与既有 loader 测试全部通过（`go test ./internal/web/ ./internal/knowledgebase/ ./internal/config/`）：

- 外部 JSON 覆盖：`TestKnowledgeBaseLoadsExternalJSONCatalog` → ready + 外部 4 条目 + 详情页含 `agent:test` sources 与"来源与生成信息"。
- legacy Markdown 回退：`TestKnowledgeBaseLegacyMarkdownLoadsAsLegacyUnknown` → 详情页 `legacy-unknown` + "旧 Markdown 未声明 safety；命令按至少 manual-gated 强度处理"。
- 缺失文件：`TestKnowledgeBaseMissingPathShowsUnavailableDiagnostic` → `unavailable` + `no such file` + 零条目。
- 不兼容文件：`TestKnowledgeBaseIncompatibleFileShowsUnavailableDiagnostic` → `unavailable` + `catalog JSON 无效` + 零条目。
- 既有 `TestLoadJSONRejectsInvalidSource` 等 loader 级诊断测试不变全绿；`TestReportPageVulnerabilityAggregateExplainsDisabledCatalog` 适配后维持 disabled 分支断言。

### 文档核查（grep 证据）

```text
$ grep -n "Pentest-Playbook" config/default.yaml.example  → 无输出
$ grep -n "Pentest-Playbook" internal/web/templates/config.html  → 无输出
$ grep -n "Pentest-Playbook" README.md docs/deploy.md  → 无输出
```

仓库内其余 `Pentest-Playbook` 引用仅存在于：spec/plans/tickets（协议溯源文档）、注释（`package_smoke_test.go`、`catalog_drift_test.go` 的 producer 来源说明）与 testdata README —— 均为来源可追溯记录，不是路径配置。`node scripts/check_markdown_links.mjs`（doc-check）通过。

### 手工验收记录（curl 证据；fake runner 或既有链路，未运行真实扫描器）

1. `/kb` 列表与详情（包内默认 catalog）：`ready` + 188 唯一条目；`/kb/snmp` 显示"复核状态：needs-review"、`Safety mode：safe`、修复建议，以及新增审计块 `Generated：agent:migration-script @ 2026-08-04`（新 build 复验）。
2. 外部 JSON 覆盖：配置指向 testdata `catalog-v2.json` → `ready` + 4 个外部条目（smb-signing/nuclei-code/test-file-delete/no-verify），证明外部路径优先于包内默认。
3. legacy Markdown 回退：配置指向 `handbook-v2.md` → `ready`；详情页 `Safety mode：legacy-unknown` + "旧 Markdown 未声明 safety" + "原始命令已由服务端门禁隐藏"。
4. safe 命令直通 / 受控命令门禁确认流：`scripts/ticket-04-web-smoke.mjs`（fake runner + sqlite seed + Playwright Chromium headless）重跑通过，产物重新生成于 `docs/reports/ticket-04-playwright/`（23:01）：`safe-command.png`（safe 直通）、optional/manual/needs-review before/after 截图、`result.txt` = `PASS: safe, optional, manual-gated, and needs-review command flows`；`console.log` 仅预期 428 gate 响应。
5. 报告 enrich：seed `run-enrich`（nuclei finding `snmpv1-community-detect-string`，包内 catalog），`GET /reports/run-enrich?view=vulnerabilities` 渲染"SNMP 公共团体字符串（高危）"、KB 漏洞描述（SNMP v1/v2c 默认团体字符串…）、修复建议（"1. 升级到 SNMP v3…"）与资产 `192.0.2.77:161/udp`。
6. 缺失 catalog 诊断：配置指向不存在文件 → `unavailable` + `open .../no-such-catalog.json: no such file or directory` + 零条目。
7. 不兼容文件诊断：配置指向非 JSON 文件 → `unavailable` + `catalog JSON 无效` + 零条目（不回退到未知副本）。

## 模拟 / 未实测分列

**已实测（自动断言驱动）：** `make test`、`go vet ./...`、`make pr-check` 全链路；上述全部 handler/config/package/drift 测试；Playwright 两条 smoke（web-smoke、ticket-04-web-smoke）；手工 curl 验收 1-7。

**未实测（需编排方终验）：**
- 归档在 linux/windows 平台的解压启动（本环境为 darwin/arm64；package-smoke 的归档组装逻辑与平台无关，windows 仅二进制名差异，未实跑）。
- 截图/trace 的人工目视复核（模型不支持读图；截图内容由 smoke 的 DOM 断言间接保证，同 ticket 04 口径）。
- 真实扫描器执行（本票非目标；所有命令仅生成不运行）。

**说明与降级：** 无。唯一环境相关项为 `make pr-check` 首次失败——`web-smoke.mjs` 的 config 注入正则与 example 新增注释行耦合（`knowledge_base:` 与 `path:` 之间出现注释），属本票样例格式变更引发的测试脚本适配，已修复并全量复跑通过，非环境阻塞。

## 审查与剩余项

- 未执行 git commit/push；未修改 `~/DEV/Pentest-Playbook`；未移除 Markdown loader（非目标）。
- 行为契约逐条核对：发行物含版本匹配 catalog v2 ✓；默认配置指向包内路径 ✓；外部 JSON/Markdown 仍可配置 ✓；缺失/不兼容显示明确 unavailable 诊断且不回退 ✓；配置页/默认样例/部署文档说明协议版本、外部更新、safety/status/legacy 边界与恢复 ✓；发行 catalog 可追溯到 producer checksum 且运行时无外部仓库访问 ✓。
- 残余风险：升级用户旧配置无 `knowledge_base.path` 时知识库保持禁用（部署文档已说明，不静默改用新归档）；`config/catalog.json` 与 `testdata` fixture 为两份字节相同拷贝，由 `catalog_drift_test.go` 双锁（字节一致 + 固定 checksum）防止漂移。
