## 1. 固定现有扫描准备契约

- [x] 1.1 为共享准备边界补充失败优先的测试，覆盖配置默认值、显式覆盖、目标/端口解析、项目排除、规则加载和预检诊断。
- [x] 1.2 补充 CLI 与 Web 特征测试，固定当前必填校验、错误信息、预检呈现、HTTP 状态和扫描未启动条件。

## 2. 建立最小共享边界

- [x] 2.1 解除 `internal/preflight` 对 `internal/app` 工具值类型的反向依赖，并用现有低层包或类型别名保持改动最小。
- [x] 2.2 在 `internal/app` 实现单一扫描准备函数，复用现有 config、target、ports 和 preflight 逻辑生成结构化结果与 `ScanOptions`。

## 3. 迁移交付入口

- [x] 3.1 迁移 CLI `scan` 命令调用共享准备边界，保留 flag、默认路径、日志、同步执行、HTML 后处理和标准输出行为。
- [x] 3.2 迁移 Web 扫描创建调用共享准备边界，保留项目必填、默认值优先级、表单重绘、Manager 启动和重定向行为。
- [x] 3.3 删除已无调用方的重复组装与排除 helper，确认不存在第二条扫描准备路径。

## 4. 兼容性验证

- [x] 4.1 运行 `go test ./...`，并比较 CLI/Web 对等输入生成的标准化扫描计划。
- [x] 4.2 运行现有扫描端到端或冒烟检查和 `make package`，确认 CLI、Web、数据库、报告、工具顺序、事件、取消及产物契约均未变化。
- [x] 4.3 确认 `go.mod`/`go.sum` 和前端依赖未变化，且现有用户 UI 设计文件未被修改。
