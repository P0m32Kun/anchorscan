## 1. 端口格式与项目模板

- [x] 1.1 用失败测试覆盖 `top1000` 先解析后排除、仅支持 `top1000`/`full`/数字 CSV，以及高危列表仅作为 CSV 插入。

## 2. run 目录与项目历史

- [x] 2.1 用失败测试覆盖报告和过程文件同目录，并在扫描历史展示关联项目。

## 3. 验证器路由

- [x] 3.1 用配置/匹配测试固定 NSE 非 Web、nuclei Web 的互斥路由，并同步用户可见说明。
